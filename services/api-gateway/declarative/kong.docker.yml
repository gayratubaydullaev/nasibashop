_format_version: "3.0"

# Сервисы в одной сети Docker: upstream — имя сервиса (см. docker-compose.services.yml).
# Порты: user 8080, product 8083, order 8081, payment 8082, delivery 8085, search 8086, notification 8087, media 8088.
# В .env укажите: KONG_DECLARATIVE_CONFIG_FILE=./services/api-gateway/declarative/kong.docker.yml
#
# JWT (RS256): плагин jwt на защищённых префиксах; iss в токене = nasibashop-user-service (см. user-service JWT_ISSUER).
# Ключи только для dev: declarative/dev-not-for-prod/ — см. README там.

consumers:
  - username: nasibashop-dev
    jwt_secrets:
      - algorithm: RS256
        key: nasibashop-user-service
        rsa_public_key: |
          -----BEGIN PUBLIC KEY-----
          
          YwIDAQAB
          -----END PUBLIC KEY-----

services:
  - name: user-service-auth
    url: http://user-service:8080/auth
    routes:
      - name: user-auth
        paths:
          - /api/auth
        strip_path: true

  - name: user-service-users
    url: http://user-service:8080/users
    routes:
      - name: user-users
        paths:
          - /api/users
        strip_path: true
        plugins:
          - name: jwt
            config:
              uri_param_names: []
              header_names:
                - authorization
              claims_to_verify:
                - exp
              key_claim_name: iss
          - name: rate-limiting
            config:
              second: 30
              policy: local
              limit_by: consumer
              fault_tolerant: true

  - name: product-service-products
    url: http://product-service:8083/products
    routes:
      - name: product-products
        paths:
          - /api/products
        strip_path: true

  - name: product-service-categories
    url: http://product-service:8083/categories
    routes:
      - name: product-categories
        paths:
          - /api/categories
        strip_path: true

  - name: order-service
    url: http://order-service:8081
    routes:
      - name: order-api
        paths:
          - /api/orders
        strip_path: false
        plugins:
          - name: jwt
            config:
              uri_param_names: []
              header_names:
                - authorization
              claims_to_verify:
                - exp
              key_claim_name: iss
          - name: rate-limiting
            config:
              second: 30
              policy: local
              limit_by: consumer
              fault_tolerant: true

  - name: payment-service
    url: http://payment-service:8082
    routes:
      - name: payment-provider-callbacks
        paths:
          - /api/payments/payme/callback
          - /api/payments/click/callback
          - /api/payments/uzcard/callback
        strip_path: false
        methods:
          - POST
        regex_priority: 100
      - name: payment-api
        paths:
          - /api/payments
        strip_path: false
        plugins:
          - name: jwt
            config:
              uri_param_names: []
              header_names:
                - authorization
              claims_to_verify:
                - exp
              key_claim_name: iss
          - name: rate-limiting
            config:
              second: 30
              policy: local
              limit_by: consumer
              fault_tolerant: true

  - name: delivery-service
    url: http://delivery-service:8085/delivery
    routes:
      - name: delivery-api
        paths:
          - /api/delivery
        strip_path: true

  - name: search-service
    url: http://search-service:8086
    routes:
      - name: search-api
        paths:
          - /api/search
        strip_path: false

  - name: notification-service
    url: http://notification-service:8087
    routes:
      - name: notification-rest
        paths:
          - /api/notifications
        strip_path: false
        plugins:
          - name: jwt
            config:
              uri_param_names: []
              header_names:
                - authorization
              claims_to_verify:
                - exp
              key_claim_name: iss
          - name: rate-limiting
            config:
              second: 30
              policy: local
              limit_by: consumer
              fault_tolerant: true
      - name: notification-ws
        paths:
          - /ws/notifications
        strip_path: false
      - name: notification-socket-io
        paths:
          - /socket.io
        strip_path: false

  - name: media-service
    url: http://media-service:8088/media
    routes:
      - name: media-mutations
        paths:
          - /api/media
        strip_path: true
        methods:
          - POST
          - DELETE
          - PUT
          - PATCH
        plugins:
          - name: jwt
            config:
              uri_param_names: []
              header_names:
                - authorization
              claims_to_verify:
                - exp
              key_claim_name: iss
          - name: rate-limiting
            config:
              second: 30
              policy: local
              limit_by: consumer
              fault_tolerant: true
      - name: media-read
        paths:
          - /api/media
        strip_path: true
        methods:
          - GET
          - HEAD

  # Явно отвечаем на GET /, иначе Kong: «no Route matched with those values»
  - name: gateway-root
    url: http://127.0.0.1:9
    read_timeout: 1
    write_timeout: 1
    connect_timeout: 1
    routes:
      - name: gateway-root-exact
        paths:
          - ~/^/$
        methods:
          - GET
          - HEAD
        strip_path: false
        plugins:
          - name: request-termination
            config:
              status_code: 200
              content_type: application/json; charset=utf-8
              body: '{"name":"nasibashop","gateway":true,"paths":["/api/auth","/api/users","/api/products","/api/categories","/api/orders","/api/payments","/api/delivery","/api/search","/api/notifications","/api/media","/ws/notifications","/socket.io"]}'

plugins:
  - name: cors
    config:
      origins:
        - http://localhost:3000
        - http://localhost:3001
        - http://127.0.0.1:3000
        - http://127.0.0.1:3001
        - http://host.docker.internal:3000
        - http://host.docker.internal:3001
        - https://shop.blungur.store
      methods:
        - GET
        - HEAD
        - POST
        - PUT
        - PATCH
        - DELETE
        - OPTIONS
      headers:
        - Accept
        - Authorization
        - Content-Type
        - X-Request-Id
      exposed_headers:
        - X-Request-Id
      credentials: true
      max_age: 3600

  - name: correlation-id
    config:
      header_name: X-Request-Id
      generator: uuid
      echo_downstream: true

  - name: rate-limiting
    config:
      second: 100
      policy: local
      limit_by: ip
      fault_tolerant: true
